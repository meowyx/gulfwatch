# Rust
/target

# Node (teammate's web folder)
web/node_modules
web/.next
web/.env.local

# General
.env
.DS_Store

# Local planning mirror (source of truth lives in Obsidian)
plan.md

# Scratch space for research, sample fetches, throwaway experiments.
# The whole directory is ignored — create it ad-hoc when needed.
tmp/

# Private handoff notes, never pushed.
handoff/
